# بناء APK بدون Android Studio — عبر GitHub (بالسحابة)

هاد بيبني الـ APK على سيرفرات GitHub مجاناً، وأنت بس بتحمّل النتيجة. جهازك ما بيتعب إطلاقاً.

## الخطوات

### 1) اعمل حساب GitHub (لو ما عندك)
روح **github.com** → Sign up → اعمل حساب مجاني (إيميل + كلمة سر).

### 2) اعمل مستودع (Repository) جديد
- بعد تسجيل الدخول، اضغط الزر **+** فوق يمين الصفحة → **New repository**.
- اكتب اسم متل `dental-clinic-app`.
- خليه **Public** أو **Private** (Private أفضل لخصوصية بياناتك، متاح مجاني).
- **لا تحط أي علامة صح** على "Add README" أو غيره.
- اضغط **Create repository**.

### 3) ارفع ملفات المشروع
- بالصفحة يلي بتفتح، رح تشوف رابط **"uploading an existing file"** — اضغطه.
- فك ضغط ملف `DentalClinicApp.zip` عندك بالكمبيوتر أول (يمين كليك → Extract / استخراج).
- اسحب **كل محتويات** مجلد `DentalClinicApp` (مو المجلد نفسه، محتوياته: app، gradle، build.gradle.kts... إلخ) وأفلتها بصفحة الرفع.
- تحت بالصفحة اكتب أي وصف بسيط بخانة "Commit changes" واضغط الزر الأخضر **Commit changes**.

### 4) خلي GitHub يبني الـ APK تلقائياً
- روح لتبويب **Actions** فوق الصفحة (جنب Code, Issues...).
- رح تشوف عملية بناء (workflow) اسمها **Build APK** شغالة أو خلصت — اضغط عليها.
- استنى لحتى تخلص (دائرة خضراء ✓ = نجحت، لونها أحمر ✗ = فيها خطأ).
- لو نجحت: انزل لتحت لقسم **Artifacts** واضغط **app-debug-apk** عشان تنزّله كملف مضغوط.
- فك ضغط الملف يلي نزلته، جواته رح تلاقي **app-debug.apk** — انسخه عالموبايل ونصّبه مباشرة.

### لو طلع خطأ (✗ أحمر بتبويب Actions)
اضغط على العملية الفاشلة، وبتشوف تفاصيل الخطأ بالأسطر الحمراء. انسخلي النص وابعتيه وبساعدك نحلها.

---

**ملاحظة**: هاد بيبني نسخة "debug" (تجريبية) من التطبيق — كافية تماماً للاستخدام الشخصي والتجربة بالعيادة. أي تعديل تعمله على الكود لاحقاً وترفعه (Commit)، رح يعيد GitHub يبني نسخة APK جديدة تلقائياً بكل مرة.
