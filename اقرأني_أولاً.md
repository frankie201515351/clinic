# تطبيق عيادة الأسنان — تعليمات التشغيل

## كيف تفتحه
1. فك ضغط الملف (unzip) بأي مكان على جهازك.
2. افتح **Android Studio** → File → Open → اختر مجلد `DentalClinicApp`.
3. استنى Gradle يخلص التحميل والمزامنة أول مرة (محتاج إنترنت، وياخد كذا دقيقة).
4. اضغط زر **Run** (▶) وهو موصول موبايل أندرويد فعلي بكيبل USB مع تفعيل "خيارات المطور" و"USB debugging"، أو استخدم محاكي (Emulator) من داخل Android Studio.

## حسابات الدخول الافتراضية (تنعمل تلقائياً أول مرة)
- **admin / admin123** → وصول كامل (شامل الحسابات، المخزون، الأرباح)
- **employee / emp123** → وصول للمرضى والمواعيد فقط

غيّرها من شاشة الإعدادات أو مباشرة من قاعدة البيانات لاحقاً — حالياً التعديل غير متاح من واجهة التطبيق، لازم تُضاف شاشة "تغيير كلمة السر" (ممكن نضيفها لاحقاً).

## كيف تطلع ملف APK جاهز للتنصيب
داخل Android Studio: **Build → Build App Bundle(s) / APK(s) → Build APK(s)**
بعد ما يخلص البناء رح يطلعلك إشعار بمكان الملف (عادة داخل `app/build/outputs/apk/debug/app-debug.apk`) — انسخه على الموبايل ونصّبه مباشرة.

## إذا طلعت أخطاء أول ما تفتحه
هاد أشيع سبب للأخطاء بمشروع جديد بالكمبيوتر:
- **"SDK not found"** → روح Android Studio → Settings → Languages & Frameworks → Android SDK، وتأكد إنه SDK 34 منزّل.
- **"Gradle sync failed"** → تأكد إنك متصل بالإنترنت (المشروع بيحمّل مكتبات أول مرة)، وجرب File → Sync Project with Gradle Files.
- أي خطأ تاني، انسخ رسالة الخطأ كاملة وابعتلي ياها وبساعدك تحلها.

## ملاحظات مهمة
- التطبيق **مستقل تماماً** عن نسخة اللابتوب (Flask) اللي بنيناها سابقاً — بيانات منفصلة، ما فيه مزامنة بينهم.
- قاعدة البيانات محلية بالكامل على الموبايل (SQLite)، ما بترسل أي بيانات لأي سيرفر خارجي.
- هاد إصدار أساسي شغال بكل الميزات الرئيسية، بس ممكن يحتاج تلميع وتحسينات شكل لاحقاً — أخبرني شو بدك تضيف أو تعدّل وبساعدك خطوة خطوة.

## الميزات المنفذة
- تسجيل دخول بصلاحيات (admin / employee)
- المرضى: إضافة، بحث، ملف تفصيلي
- المواعيد: إضافة وحذف، أزرار تاريخ سريعة
- مخطط الأسنان التفاعلي (32 سن) مع حالة كل سن وملاحظات
- صور المرضى (رفع من المعرض + ملاحظة)
- الحسابات المالية (رصيد مستحق لكل مريض بالليرة والدولار)
- المخزون (تنبيه عند نقص الكمية)
- الأرباح الشهرية
- الإعدادات (ساعات الدوام، أيام العطلة، سعر الصرف، تسجيل الخروج)
